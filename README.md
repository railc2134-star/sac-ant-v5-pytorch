# MuJoCo Ant Trainer

A reinforcement learning project that trains an ant to walk using MuJoCo and PyTorch.

## Technologies Used
* Python
* PyTorch
* MuJoCo
* Gymnasium

## How to Run
1. Install required packages: `pip install torch gymnasium numpy`
2. Run the project: `python mujuco_ant.py`